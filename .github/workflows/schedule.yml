name: ScheduleTest

on:
  pull_request:
  schedule:
    - cron: "*/5 * * * *"

jobs:
  translation:
    runs-on: ubuntu-latest
    strategy:
      max-parallel: 4
    steps:
    - name: Print
      run: echo "Hello scheduled test"
